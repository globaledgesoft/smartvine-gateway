/**
 * Created by dineshbabuc on 16/3/15.
 */
var udpserver = require("../index.js")

var conf ={
    host : "0.0.0.0",
    port : 6666,
    handler : ""
}

var udp = new udpserver(conf);

udp.connect()